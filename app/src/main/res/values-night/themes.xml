<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Dark application theme -->
    <style name="Theme.Cupcake" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/primary_dark_theme</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryDark</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color -->
        <item name="colorSecondary">@color/accent</item>
        <item name="colorSecondaryVariant">@color/accent</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Surface colors -->
        <item name="colorSurface">@color/background_primary</item>
        <item name="colorOnSurface">@color/text_primary</item>
        <item name="colorOnSurfaceVariant">@color/text_secondary</item>
        <!-- Container colors -->
        <item name="colorSecondaryContainer">@color/accent_background</item>
        <item name="colorOnSecondaryContainer">@color/white</item>
        <item name="colorPrimaryContainer">@color/accent</item>
        <item name="colorOnPrimaryContainer">@color/dark</item>
        <!-- Status bar color -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>
        <item name="android:windowLightStatusBar">false</item>
        <!-- Text colors -->
        <item name="android:textColorPrimary">@color/light</item>
        <item name="android:textColorSecondary">@color/secondary</item>
        <item name="toolbarStyle">@style/Widget.App.Toolbar.DarkTheme</item>
        <item name="actionBarStyle">@style/Widget.App.Toolbar.DarkTheme</item>
        <item name="colorNoResults">@color/accent</item>
        <item name="colorEmptyState">@color/light</item>
        <item name="colorError">@color/design_default_color_error</item>
        <item name="drawerArrowStyle">@style/DrawerArrowStyle</item>
        <!-- Set popup menu theme -->
        <item name="popupMenuStyle">@style/Widget.App.PopupMenu.DarkTheme</item>
        <item name="actionOverflowMenuStyle">@style/Widget.App.PopupMenu.DarkTheme</item>
    </style>

    <style name="DrawerArrowStyle" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="color">@color/white</item>
    </style>

    <style name="Widget.App.Toolbar.DarkTheme" parent="Widget.MaterialComponents.Toolbar.Primary">
        <item name="titleTextColor">@color/white</item>
        <item name="subtitleTextColor">@color/white</item>
        <item name="colorControlNormal">@color/white</item>
    </style>

    <!-- Popup menu theme for dark mode (white text) -->
    <style name="Widget.App.PopupMenu.DarkTheme" parent="Widget.MaterialComponents.PopupMenu">
        <item name="android:textColor">@color/white</item>
        <item name="android:itemBackground">@color/dark</item>
    </style>

    <!-- Theme for popup menu overlays -->
    <style name="Theme.Cupcake.PopupOverlay" parent="ThemeOverlay.MaterialComponents.Dark">
        <item name="android:textColor">@color/white</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/light</item>
    </style>

    <style name="Theme.Cupcake.NoActionBar" parent="Theme.Cupcake">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Theme for AppBar overlays -->
    <style name="Theme.Cupcake.AppBarOverlay" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar">
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/light</item>
        <item name="colorControlNormal">@color/white</item>
    </style>

    <style name="FullScreenDialogStyle" parent="Theme.Cupcake">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowBackground">@color/background_primary</item>
        <item name="android:statusBarColor">?attr/colorPrimary</item>
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
        <item name="android:windowSoftInputMode">adjustResize</item>
        <item name="colorControlNormal">@color/white</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorPrimary">@color/primary_dark_theme</item>
    </style>
</resources>